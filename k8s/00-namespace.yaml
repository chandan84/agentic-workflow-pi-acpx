---
apiVersion: v1
kind: Namespace
metadata:
  name: pi-agents
  labels:
    app.kubernetes.io/part-of: pispec-flow
    app.kubernetes.io/managed-by: kubectl
  annotations:
    # PiSpec Flow platform namespace
    # All agent pods, services, and storage resources reside here
    description: "PiSpec Flow — multi-agent PI spec workflow platform"
