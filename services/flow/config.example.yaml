service: flow
env: dev
grpc_addr: ":7002"
http_addr: ":7102"
log_level: info
log_format: json
postgres:
  dsn: postgres://awpa:awpa@localhost:5432/awpa?sslmode=disable
  schema: flows
nats:
  url: nats://localhost:4222
  stream: AWPA
auth:
  mode: none
features:
  prompt_generator_enabled: true
stubs:
  store:   real
  bus:     real
  runtime: stub
